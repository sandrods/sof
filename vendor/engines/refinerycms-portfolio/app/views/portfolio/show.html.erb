<% content_for :title, @portfolio_entry.title %>

<h1>
  <span><%= @master_entry.title %></span>
</h1>


	<div id="galleria">
	<% @portfolio_entry.images.each do |image| %>
		<%= link_to(image_fu(image, '96x96#c'), image.thumbnail("900x900>").url ) %>
	<% end %>
	</div>

	<script>
	    $('#galleria').galleria({
	        height:600,
			transition: 'fade'
	    });
	</script>


<% content_for :javascripts, javascript_include_tag('galleria-1.2.4') + javascript_include_tag('galleria.classic') %>
