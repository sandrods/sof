<h2><%= t('categorias.index.title') %></h2>
<ul id='categories'>
  <% @categorias.each do |categoria| %>
    <li<%= " class='selected'" if @category.present? and @category.id == categoria.id %>>
      <%= link_to categoria.titulo, categoria_url(categoria) %>
    </li>
  <% end %>
</ul>
