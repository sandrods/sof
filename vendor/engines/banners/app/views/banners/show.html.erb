<% content_for :body_content_title do %>
  <%= @banner.titulo %>
<% end %>

<% content_for :body_content_left do %>
  <section>
    <h1>Titulo</h1>
    <p>
      <%=raw @banner.titulo %>
    </p>
  </section>
  <section>
    <h1>Descricao</h1>
    <p>
      <%=raw @banner.descricao %>
    </p>
  </section>
  <section>
    <h1>Url</h1>
    <p>
      <%=raw @banner.url %>
    </p>
  </section>
  <section>
    <h1>Image</h1>
    <p>
      <%= image_fu @banner.image, nil %>
    </p>
  </section>
<% end %>

<% content_for :body_content_right do %>
  <aside>
    <h2><%= t('.other') %></h2>
    <ul id="banners">
      <% @banners.each do |banner| %>
        <li>
          <%= link_to banner.titulo, banner_url(banner) %>
        </li>
      <% end %>
    </ul>
  </aside>
<% end %>

<%= render :partial => "/shared/content_page" %>
